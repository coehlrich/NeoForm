plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'neoform'

['client', 'joined', 'server'].each {
    include(it)
    project(':' + it).projectDir = file('projects/' + it)
}