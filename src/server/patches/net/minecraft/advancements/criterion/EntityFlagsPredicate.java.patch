--- a/net/minecraft/advancements/criterion/EntityFlagsPredicate.java
+++ b/net/minecraft/advancements/criterion/EntityFlagsPredicate.java
@@ -67,7 +_,7 @@
         } else {
             return this.isFallFlying.isPresent() && entity instanceof LivingEntity living && living.isFallFlying() != this.isFallFlying.get()
                 ? false
-                : !(this.isBaby.isPresent() && entity instanceof LivingEntity living) || living.isBaby() == this.isBaby.get();
+                : !(this.isBaby.isPresent() && entity instanceof LivingEntity livingx) || livingx.isBaby() == this.isBaby.get();
         }
     }
 
