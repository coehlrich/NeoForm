--- a/net/minecraft/client/gui/screens/worldselection/WorldOpenFlows.java
+++ b/net/minecraft/client/gui/screens/worldselection/WorldOpenFlows.java
@@ -436,7 +_,7 @@
             this.minecraft
                 .execute(
                     () -> {
-                        if (e.getCause() instanceof CowFSSymlinkException) {
+                        if (ex.getCause() instanceof CowFSSymlinkException) {
                             this.minecraft
                                 .setScreenAndShow(
                                     new AlertScreen(
@@ -457,7 +_,7 @@
                             new AlertScreen(
                                 cleanup,
                                 Component.translatable("upgradeWorld.failed_cleanup.title"),
-                                Component.translatable("upgradeWorld.failed_cleanup.message", Component.literal(e.newWorldFolderName()).withColor(-8355712))
+                                Component.translatable("upgradeWorld.failed_cleanup.message", Component.literal(exx.newWorldFolderName()).withColor(-8355712))
                             )
                         )
                 );
